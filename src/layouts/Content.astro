---
import Base from "./Base.astro";

export interface Props {
    title: string;
}

const { title } = Astro.props;
---

<Base title={title}>
    <main>
        <slot />
    </main>
</Base>

<style>
    main {
        margin: auto;
        padding: 1.5rem;
        max-width: 60ch;
    }
</style>
